- [aello overview](aello-overview.md) — what aello is, current state (v0.2.0 = first stable line), repo, install, docs site URL; TWO agents since 2026-08-06 (Claude Code + Cline), so "Claude-only, no API keys" is no longer true
- [aello architecture decisions](aello-architecture-decisions.md) — the non-obvious WHYs (auth, /sync, attribution); capabilities are now three ROLES (v0.2.0); personas are coder/none/custom with the generation in a per-env sidecar (v0.2.8), accepted personas: TechnicalDirector gen1, CleaningCompanyMainDev gen1 (both 2026-08-03)
- [aello CI + release](aello-ci-release.md) — auto patch-bump only when the version is already tagged (a hand-set version ships as written), 4 targets, both a vX.Y.Z tag and rolling latest, never draft a release, CI never touches Cargo.lock
- [aello contextdb](aello-contextdb.md) — PostCompact is dormant not broken, SessionEnd does everything, transcripts are copied (not referenced), thinking is captured as summaries and raw reasoning is unextractable, summarized display IS the capture switch, and where a session's tokens actually go (tool results 56%, prose 4.3%)
- [aello TUI design](aello-tui-design.md) — "Kinetic Command" palette + conventions
- [working style](working-style.md) — user does not read plans, give decisions to choose from; conciseness + anti-sycophancy are universal and enforced structurally, not remembered
- [aello dev gotchas](aello-dev-gotchas.md) — Windows locked-exe install fix; print mode skips memory but does fire hooks; universal skills don't backfill and an older binary undoes a hand-backfill; aello update can't ship branch work; gh issue view needs --json; Claude cwd-encoding folds all non-alphanumerics to '-'; config.toml has an extra config/ level; SessionStart additionalContext works; screenshotting a page without downloading a browser; resuming through aello re-places the env; backfilling a hook is file + settings registration; claude_md is now coder/none/custom (+path) with generation in persona.gen; where the env inventory lives; Cargo.lock re-dirties itself in seconds (use --autostash); the docs site is generated from docs/; .gitignore does nothing to an already-tracked file; Python glob skips dotted dirs so a fleet scan silently finds nothing; a pre-0.2.8 binary hard-fails on a migrated config and this machine's is now migrated on disk; 39 blueprints vs 41 env dirs; how to mine contextdb without a truncated dump; the 41 envs live in TWO Desktop trees and scanning only Desktop\work silently reports 18; one env path has a space and an unquoted shell loop silently skips it (40 of 41); a stray \s in a bundled hook's docstring warns on stderr in every env; Git Bash rewrites a leading-slash arg into a Windows path and can make a failing test PASS (use MSYS_NO_PATHCONV=1); a new Windows User env var never reaches a running process so a fleet switch can be on and inert; *.NOTE.md and *.HANDOFF.md ARE gitignored in this repo though CLAUDE.md says otherwise; crossterm raw mode succeeds on a PIPED stdin so gate on is_terminal() not on raw mode; three encoding traps where the test agreed with the bug (BOM-less UTF-16 is valid UTF-8, sys.stdin uses surrogateescape so json.load never raises, subprocess text=True double-encodes); and open(path,'w') truncates before it writes, which emptied a 27KB memory file mid-/sync
- [cline](cline.md) — the CLI not the extension; isolation is `--config`/`--data-dir` FLAGS not env vars (measured, and `~/.cline` stays untouched), AGENTS.md not CLAUDE.md, no memory and no ignore file; the claude-code provider AUTHENTICATES on aello's shared token but then cannot use tools at all, so on a subscription Cline is chat-only and real work needs a metered provider; only TaskStart fires and only from `<config>/hooks/`, with identifiers and no content — so voice, the per-turn rules and contextdb capture cannot be ported as hooks; SHIPPED INTO aello 2026-08-06 as `--agent cline` with a metered login the user chose knowingly, and the one trap is that hand-writing providers.json half-works because Cline strips the apiKey on its next run — install it with `cline auth`
- [aello open source](aello-open-source.md) — license/contributor/domain decisions; stay pseudonymous; Tier-1 done
- [aello voice capability](aello-voice-capability.md) — where revoiced lives, the shared state.json contract, edge-tts is global, voice is now unconditional, vendor all FIVE hook files, HOOK_VERSION is the drift check (now 19 — 19 moved only `--status` and the bump rule now covers CLI-only changes because the digest cannot tell; 13/14 added opt-in Telegram delivery behind three env vars and made it reach already-open sessions, 17 made an empty value off, 18 records a failed send in state.json as telegram_error which aello voice status now prints; 15/16 put a REVOICED_SWEEP registry walk on the hook path to repair volumes the live-session restore can never reach, defaulting to everything below full and never touching an exact 0 — and revoiced is the SECOND ducker here, Wispr Flow is the other, which is why the damage kept returning; a stale copy damages the machine's audio by TWO different mechanisms — 10 was itself damaging — and the live-session check finds only 1 of 7 damaged entries), re-vendor = re-backfill 39 envs (not 41 — 73 .claude-env-* dirs exist, 39 are real), both writers of state.json now refuse a corrupt file, a vendored copy claims the toast identity but never the protocol; the per-turn hook carries FOUR rules and the last one fixes a response's SHAPE — one closing block, TL;DR line + 3-4 next steps beneath it, and the TL;DR must stay on ONE line or speak.py cuts its tail off plus a PreToolUse hook denying plan mode, backfilled to all 41 envs; the deny mechanism is measured, whether the plan tools reach it is not — and as of 2026-08-06 plan-blocked.log exists in NONE of the 39 envs, so it has never fired
- [aello audit 2026-08-06](aello-audit-2026-08-06.md) — the whole-codebase audit: what was fixed and verified that day, what is deliberately still open (four findings belong to vendored revoiced files, release signing, repo-root detection, the interpreter probe), and which of its unverifiable claims are now settled from data
